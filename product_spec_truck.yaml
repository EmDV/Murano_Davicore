# Murano product specification -- Davicore truck

resources:
-   alias: temperature
    format: float
-   alias: uptime
    format: integer
-   alias: GPS
    format: string
-   alias: starttime
    format: integer
-   alias: endtime
    format: integer
